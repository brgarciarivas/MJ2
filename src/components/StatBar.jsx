
import React from 'react';

import Base from './Base';

export default class StatBar extends Base {
    render() {
        return (
            <div className='StatBar'>
            	StatBar
            </div>
        );
    }
}